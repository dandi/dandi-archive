[tox]
min_version = 4.22
requires =
    tox-uv
envlist =
    lint
    # type
    test
    check-migrations
    reuse

[testenv]
runner = uv-venv-lock-runner
# Should match the one in pyproject.toml requires-python for consistency
base_python = py313
pass_env =
    DJANGO_*
extras =
    development

[testenv:lint]
package = skip
dependency_groups =
    lint
commands =
    ruff check
    ruff format --check

[testenv:format]
package = skip
dependency_groups =
    lint
commands =
    ruff check --fix-only
    ruff format

[testenv:type]
dependency_groups =
    type
    test
commands =
    mypy {posargs}

[testenv:test]
dependency_groups =
    test
commands =
    pytest {posargs}

[testenv:check-migrations]
set_env =
    DJANGO_SETTINGS_MODULE = dandiapi.settings.testing
commands =
    {envpython} ./manage.py makemigrations --check --dry-run

[testenv:reuse]
package = skip
dependency_groups =
    reuse
commands =
    reuse lint
